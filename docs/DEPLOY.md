# Руководство по развертыванию Fit-Hub на Ubuntu

## Предварительные требования

**Локальная машина:**

- Node.js (v18+) или Bun
- Git
- Доступ к репозиторию проекта

**Сервер:**

- Ubuntu сервер с root/sudo доступом
- Установленные: Git, Nginx, PM2, Node.js (v18+), npm
- PostgreSQL база данных (локальная или удаленная)
- Доменное имя: gymmy-bot.ru
- Telegram Bot Token

**⚠️ ВАЖНО:** Сборка фронтенда выполняется на локальной машине, так как сервер слабый. Собранные файлы коммитятся в git и пушатся на сервер.

## Пошаговая инструкция

### 0. Подготовка на локальной машине (первый раз)

```bash
# Клонирование репозитория (если еще не клонировали)
git clone <ваш-репозиторий> fit-hub
cd fit-hub

# Установка ВСЕХ зависимостей (включая фронтенд и инструменты сборки)
npm install
# или
npm run install:all

# Сборка фронтенда для production
npm run build

# Проверка, что dist/ создался
ls -la dist/

# Коммит и пуш собранных файлов
git add dist/
git commit -m "Initial build: добавлены собранные файлы для production"
git push origin main
```

Теперь можно переходить к настройке сервера.

### 1. Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка необходимых зависимостей
sudo apt install -y git curl build-essential

# Установка Bun (рекомендуется - быстрее и экономичнее по памяти)
curl -fsSL https://bun.sh/install | bash
source ~/.bashrc

# Проверка установки Bun
bun --version

# Альтернатива: Установка Node.js (если не хотите использовать Bun)
# Для Node.js 20.x:
# curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
# sudo apt install -y nodejs
# node --version
# npm --version

# Установка PostgreSQL (если база данных локальная)
sudo apt install -y postgresql postgresql-contrib

# Создание базы данных
sudo -u postgres psql
CREATE DATABASE fit_hub;
CREATE USER fit_hub_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE fit_hub TO fit_hub_user;
\q
```

### 2. Клонирование репозитория

```bash
# Выберите директорию для проекта (может быть любая, например /root, /home/user, /opt и т.д.)
# Например, если вы root:
cd /root

# Или если вы обычный пользователь:
cd ~

# Клонирование проекта
git clone https://github.com/your-username/fit-hub.git
# или если используете SSH:
# git clone git@github.com:your-username/fit-hub.git

# Переход в директорию проекта
cd fit-hub

# Запомните полный путь к проекту - он понадобится для настройки nginx и PM2
PROJECT_PATH=$(pwd)
echo "Путь к проекту: $PROJECT_PATH"
# Например: /root/fit-hub или /home/username/fit-hub
# Используйте этот путь во всех следующих командах вместо /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!
```

**⚠️ ВАЖНО:** Во всех следующих инструкциях замените `/path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!` на ваш реальный путь к проекту!

### 3. Проверка свободных портов

**ВАЖНО:** Перед настройкой проверьте, какие порты уже заняты другими проектами:

```bash
# Проверка занятых портов (показывает все слушающие порты)
sudo netstat -tlnp | grep LISTEN
# или более современная команда:
sudo ss -tlnp | grep LISTEN

# Или проверьте конкретные порты:
sudo lsof -i :4000
sudo lsof -i :3000
sudo lsof -i :4000
```

**Пример вывода:**

```
tcp  0  0  0.0.0.0:4000  0.0.0.0:*  LISTEN  1234/node
tcp  0  0  0.0.0.0:3000  0.0.0.0:*  LISTEN  5678/node
```

**Выберите свободный порт для API сервера:**

- По умолчанию в конфигах указан порт **4000**
- Если 4000 занят, выберите другой (например: 4001, 5000, 6000, 8000)
- Запомните выбранный порт - он понадобится в следующих шагах

**Примечание:**

- Порт 3000 обычно используется для dev-режима фронтенда - в production не нужен
- Порт для API сервера должен быть свободен и будет работать только на localhost
- Nginx будет проксировать запросы с домена gymmy-bot.ru на этот локальный порт

### 4. Настройка переменных окружения

```bash
# Создание файла .env
cp .env.example .env  # если есть пример
# или создайте новый файл
nano .env
```

Добавьте следующие переменные в `.env`:

```env
# База данных
DATABASE_URL="postgresql://fit_hub_user:your_secure_password@localhost:5432/fit_hub"

# Сервер (указать выбранный свободный порт!)
NODE_ENV=production
PORT=4000

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# OpenAI (опционально)
OPENAI_API_KEY=your_openai_api_key
OPENAI_API=https://gptunnel.ru/v1
ASSISTANT_CODE=your_assistant_code
```

**⚠️ Важно:** Убедитесь, что порт в `PORT=` совпадает с портом в `ecosystem.config.cjs` и `nginx.conf`!

Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano).

### 5. Установка зависимостей на сервере

**⚠️ ВАЖНО:** Сборка фронтенда выполняется на локальной машине и коммитится в git, так как сервер слабый и не может собрать проект.

**Выберите один из вариантов:**

#### Вариант 1: С Bun (РЕКОМЕНДУЕТСЯ ⭐)

**Преимущества Bun:**

- ⚡ Установка за секунды (вместо минут)
- 💾 Потребляет 200-300 МБ ОЗУ (вместо 1-2 ГБ у npm)
- 🚀 Работает стабильно даже на слабых серверах
- ✅ Отлично работает с Prisma

```bash
cd /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь к проекту!

# Получение последних изменений из git (включая собранные файлы)
git pull origin main  # или ваша ветка

# Установка ТОЛЬКО production зависимостей (бекенд пакеты)
# Это установит только: express, cors, dotenv, grammy, luxon, node-cron, pg, prisma и их зависимости
# Фронтенд пакеты (react, vite, tailwind и т.д.) НЕ будут установлены - экономия ресурсов!
bun install --production

# Генерация Prisma клиента (обязательно!)
bunx prisma generate

# Применение миграций базы данных
bunx prisma migrate deploy

# (Опционально) Заполнение начальными данными
bun prisma/seed.ts

# Проверка, что папка dist существует (она должна быть из git)
ls -la dist/
```

#### Вариант 2: С npm (альтернатива)

**⚠️ Если сервер слабый, может потребоваться Swap-файл (см. ниже)**

```bash
cd /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь к проекту!

# Получение последних изменений из git (включая собранные файлы)
git pull origin main  # или ваша ветка

# Установка ТОЛЬКО production зависимостей (бекенд пакеты)
# Это установит только: express, cors, dotenv, grammy, luxon, node-cron, pg, prisma и их зависимости
# Фронтенд пакеты (react, vite, tailwind и т.д.) НЕ будут установлены - экономия ресурсов!
npm install --production

# Команда для установки с минимальным риском падения:
npm cache clean --force
NODE_OPTIONS="--max-old-space-size=512" npm install --production --no-optional --prefer-offline

# ⚠️ ВАЖНО: tsx нужен для запуска TypeScript
# Вариант 1: Установить глобально (рекомендуется для сервера)
npm install -g tsx

# Вариант 2: Использовать npx (работает без глобальной установки, но медленнее)
# PM2 будет использовать npx tsx из ecosystem.config.cjs

# Генерация Prisma клиента (обязательно!)
npx prisma generate

# Применение миграций базы данных
npx prisma migrate deploy

# (Опционально) Заполнение начальными данными
npx tsx prisma/seed.ts

# Проверка, что папка dist существует (она должна быть из git)
ls -la dist/
```

**Если папки `dist/` нет** - значит сборка не была закоммичена. Смотрите раздел "Обновление проекта" про сборку на локальной машине.

#### Создание Swap-файла (если используете npm и сервер всё равно "убивает" процесс)

Если даже с `npm install --production` процесс завершается с ошибкой "Killed", создайте Swap-файл:

```bash
# Создание Swap-файла на 2 ГБ
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# Добавление в fstab для автозагрузки при перезагрузке
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# Проверка
free -h
```

**Рекомендация:** Если у вас слабый сервер (менее 1 ГБ ОЗУ), лучше использовать Bun вместо npm - он решит проблему без Swap-файла.

### 6. Настройка PM2

**⚠️ Перед запуском:** Убедитесь, что порт в `ecosystem.config.cjs` совпадает с портом в `.env` файле!

```bash
# Если нужно изменить порт в ecosystem.config.cjs:
nano ecosystem.config.cjs
# Измените PORT: 4000 на ваш выбранный порт
```

```bash
# Создание директории для логов PM2
sudo mkdir -p /var/log/pm2
sudo chown -R $USER:$USER /var/log/pm2

# Копирование конфигурации PM2 (если нужно отредактировать)
# Убедитесь, что путь в ecosystem.config.cjs правильный: cwd: '/path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!'

# Запуск приложения через PM2
# Если приложение уже запущено, сначала удалите его:
pm2 delete fit-hub 2>/dev/null || true

# Запуск приложения
pm2 start ecosystem.config.cjs

# Или если нужно перезапустить:
pm2 restart ecosystem.config.cjs

# Сохранение конфигурации PM2 для автозапуска
pm2 save

# Настройка автозапуска PM2 при перезагрузке системы
pm2 startup
# Выполните команду, которую выдаст PM2 (обычно что-то вроде sudo env PATH=... pm2 startup systemd -u $USER --hp /home/$USER)

# Проверка статуса
pm2 status
pm2 logs fit-hub
```

### 7. Настройка Nginx

**⚠️ Перед настройкой:** Убедитесь, что порт в конфигурации совпадает с портом в `.env` и `ecosystem.config.cjs`!

**⚠️ ВАЖНО:** Если у вас уже есть конфигурация nginx для других проектов на портах 443 и 2020, вам нужно добавить конфигурацию для fit-hub на порту 80 (HTTP), или использовать отдельный путь/поддомен. См. пример ниже.

**Как это работает:**

- Пользователь заходит на `http://gymmy-bot.ru` (домен)
    - Браузер автоматически использует порт **80** для HTTP (стандартный)
    - Если настроен SSL, будет `https://gymmy-bot.ru` на порту **443**
- Nginx слушает на порту **80** (публичный, доступен из интернета)
- Nginx отдает статические файлы фронтенда из `/dist/client`
- Запросы на `/api/*` Nginx проксирует на `localhost:4000` (ВНУТРЕННИЙ порт, виден только на сервере)
- Пользователь **НЕ видит порт 4000** - для него все работает через домен и порт 80/443

**Разница портов:**

- **Порт 80/443** - публичные (HTTP/HTTPS), принимают запросы от пользователей
- **Порт 4000** - внутренний, работает только на localhost сервера, не доступен извне

У вас есть два варианта настройки Nginx:

#### Вариант 1: Отдельный файл конфигурации (рекомендуется)

```bash
# Копирование конфигурации Nginx
sudo cp nginx.conf.example /etc/nginx/sites-available/fit-hub
sudo nano /etc/nginx/sites-available/fit-hub
```

Проверьте в конфигурации:

- Домен уже установлен: `gymmy-bot.ru www.gymmy-bot.ru`
- Порт для proxy_pass: `http://localhost:4000` (должен совпадать с PORT в .env!)
- Проверьте пути к файлам (`/path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!/dist/client`)

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/fit-hub /etc/nginx/sites-enabled/

# Проверка конфигурации Nginx
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx

# Проверка статуса Nginx
sudo systemctl status nginx
```

#### Вариант 2: Добавление в существующий конфиг

Если у вас уже есть рабочий nginx конфиг и вы хотите добавить туда настройки:

```bash
# Откройте существующий конфиг
sudo nano /etc/nginx/sites-available/your-existing-config
# или
sudo nano /etc/nginx/nginx.conf

# Добавьте в секцию http (или в нужный server блок):
```

Добавьте следующий блок в ваш существующий конфиг:

```nginx
server {
    # Порт 80 - стандартный HTTP порт для публичного доступа
    # Пользователи заходят на http://gymmy-bot.ru (без указания порта = автоматически 80)
    # Nginx принимает запросы на порту 80 и проксирует на внутренний порт 4000 (Express сервер)
    listen 80;
    server_name gymmy-bot.ru www.gymmy-bot.ru;

    # Логи
    access_log /var/log/nginx/fit-hub-access.log;
    error_log /var/log/nginx/fit-hub-error.log;

    # Максимальный размер загружаемых файлов
    client_max_body_size 50M;

    # Статические файлы фронтенда
    location / {
        root /path/to/fit-hub/dist/client;  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!
        try_files $uri $uri/ /index.html;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # API endpoints - проксируем на Express сервер
    # localhost:4000 - ВНУТРЕННИЙ порт (виден только на сервере, не снаружи)
    # Измените порт на тот, который вы выбрали (должен совпадать с PORT в .env!)
    location /api {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}

# ⚠️ ВАЖНО: После настройки SSL сертификата (раздел 8),
# этот блок будет дополнен конфигурацией для порта 443 (HTTPS)
# Порт 80 останется для редиректа HTTP → HTTPS
```

```bash
# Проверка конфигурации Nginx
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx

# Проверка статуса Nginx
sudo systemctl status nginx
```

#### Вариант 2: Отдельный файл конфигурации

Если хотите вынести конфигурацию fit-hub в отдельный файл:

```bash
# Используйте готовый файл или скопируйте из nginx-fit-hub-config.conf
sudo nano /etc/nginx/sites-available/fit-hub

# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/fit-hub /etc/nginx/sites-enabled/

# Проверка конфигурации Nginx
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

#### Вариант 3: Использование отдельного пути (если порт 80 занят другим проектом)

Если порт 80 занят другим проектом, можно использовать отдельный путь:

### 7. Настройка файрвола (если используется UFW)

```bash
# Разрешение HTTP и HTTPS
sudo ufw allow 'Nginx Full'
# или отдельно:
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Проверка статуса
sudo ufw status
```

### 8. Настройка SSL сертификата (опционально, но рекомендуется)

```bash
# Установка Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
sudo certbot --nginx -d gymmy-bot.ru -d www.gymmy-bot.ru

# Автоматическое обновление (настроено автоматически)
sudo certbot renew --dry-run
```

После получения SSL, Certbot автоматически обновит конфигурацию Nginx для использования HTTPS.

**Примечание:** Если у вас уже есть SSL сертификат для gymmy-bot.ru (судя по вашей конфигурации на портах 443 и 2020), certbot может использовать существующий сертификат или создать новый блок. Будьте внимательны при настройке.

### 9. Проверка работы

```bash
# Проверка PM2 процессов
pm2 list
pm2 logs fit-hub --lines 50

# Проверка порта API сервера (используйте ваш порт, например 4000)
curl http://localhost:4000/api/fitness/health  # если есть health endpoint
# или проверьте, что порт слушается:
sudo netstat -tlnp | grep 4000
# или
sudo ss -tlnp | grep 4000

# Проверка Nginx
curl http://localhost
curl http://gymmy-bot.ru
curl http://gymmy-bot.ru/api/fitness/health  # проверка проксирования API
```

**🌐 Как открыть фронтенд:**

После настройки nginx, фронтенд будет доступен по адресу:

- **HTTP (без SSL):** `http://gymmy-bot.ru`
- **HTTP с www:** `http://www.gymmy-bot.ru`

**Важно:**

- Убедитесь, что nginx перезагружен: `sudo systemctl reload nginx`
- Убедитесь, что PM2 запущен: `pm2 status`
- Убедитесь, что папка `/path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!/dist/client` существует и содержит файлы

**После настройки SSL (раздел 8):**

- **HTTPS:** `https://gymmy-bot.ru` (рекомендуется)

**Если фронтенд не открывается:**

1. Проверьте логи nginx: `sudo tail -f /var/log/nginx/fit-hub-error.log`
2. Проверьте, что папка `dist/client` существует: `ls -la /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!/dist/client`
3. Проверьте, что порт 80 слушается: `sudo ss -tlnp | grep :80`
4. Проверьте, что домен указывает на ваш сервер: `ping gymmy-bot.ru`

### 10. Мониторинг и обслуживание

```bash
# Просмотр логов PM2
pm2 logs fit-hub

# Просмотр логов Nginx
sudo tail -f /var/log/nginx/fit-hub-access.log
sudo tail -f /var/log/nginx/fit-hub-error.log

# Перезапуск приложения
pm2 restart fit-hub

# Перезагрузка после изменений в коде
# ВАЖНО: Сборка делается на локальной машине (см. раздел "Обновление проекта")
cd /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь к проекту!
git pull  # получаем собранные файлы из git

# С Bun (рекомендуется):
bun install --production  # если изменились зависимости
bunx prisma generate  # если изменилась схема БД
bunx prisma migrate deploy  # если есть новые миграции

# С npm (альтернатива):
# npm install --production  # если изменились зависимости
# npx prisma generate  # если изменилась схема БД
# npx prisma migrate deploy  # если есть новые миграции

pm2 restart fit-hub
```

## Обновление проекта

### На локальной машине (перед пушем в git)

**Вариант 1: С Bun (рекомендуется)**

```bash
# Переключение в директорию проекта
cd /path/to/fit-hub

# Получение последних изменений
git pull origin main

# Установка ВСЕХ зависимостей (если изменились)
bun install

# Генерация Prisma клиента (если изменилась схема)
bunx prisma generate

# Сборка фронтенда для production
bun run build

# Проверка, что dist/ создался
ls -la dist/

# Коммит и пуш изменений (включая собранные файлы)
git add .
git commit -m "Обновление: [опишите изменения]"
git push origin main
```

**Вариант 2: С npm**

```bash
# Переключение в директорию проекта
cd /path/to/fit-hub

# Получение последних изменений
git pull origin main

# Установка ВСЕХ зависимостей (если изменились)
npm install
# или
npm run install:all

# Генерация Prisma клиента (если изменилась схема)
npx prisma generate

# Сборка фронтенда для production
npm run build

# Проверка, что dist/ создался
ls -la dist/

# Коммит и пуш изменений (включая собранные файлы)
git add .
git commit -m "Обновление: [опишите изменения]"
git push origin main
```

**Примечание:** На локальной машине нужны все зависимости для сборки фронтенда (React, Vite, Tailwind и т.д.)

### На сервере (после пуша)

**Вариант 1: С Bun (рекомендуется)**

```bash
cd /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь к проекту!

# Остановка приложения
pm2 stop fit-hub

# Получение обновлений (включая собранные файлы)
git pull origin main  # или ваша ветка

# Установка ТОЛЬКО production зависимостей (бекенд пакеты)
bun install --production

# Генерация Prisma клиента (если изменилась схема)
bunx prisma generate

# Применение миграций (если есть новые)
bunx prisma migrate deploy

# Проверка, что dist/ обновился
ls -la dist/

# Запуск приложения
pm2 restart fit-hub

# Проверка статуса
pm2 status
pm2 logs fit-hub --lines 20
```

**Вариант 2: С npm**

```bash
cd /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь к проекту!

# Остановка приложения
pm2 stop fit-hub

# Получение обновлений (включая собранные файлы)
git pull origin main  # или ваша ветка

# Установка ТОЛЬКО production зависимостей (бекенд пакеты)
npm install --production

# Генерация Prisma клиента (если изменилась схема)
npx prisma generate

# Применение миграций (если есть новые)
npx prisma migrate deploy

# Проверка, что dist/ обновился
ls -la dist/

# Запуск приложения
pm2 restart fit-hub

# Проверка статуса
pm2 status
pm2 logs fit-hub --lines 20
```

## Устранение проблем

### Проблема: PM2 не запускается или ошибка "No script path"

```bash
# Проверка, что Bun установлен и доступен
which bun
bun --version

# Проверка, что файл server/init.ts существует
ls -la /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!/server/init.ts

# Проверка текущей директории в конфиге
cat ecosystem.config.cjs | grep cwd

# Удаление старого процесса (если есть)
pm2 delete fit-hub 2>/dev/null || true

# Проверка логов
pm2 logs fit-hub --err

# Проверка переменных окружения
pm2 env 0

# Запуск заново
cd /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь к проекту!
pm2 start ecosystem.config.cjs

# Если всё равно не работает, запуск напрямую для проверки:
bun server/init.ts
# Должно запуститься без ошибок
```

**Важно:**

- Убедитесь, что файл называется `ecosystem.config.cjs` (не `.js`) из-за `"type": "module"` в package.json
- Убедитесь, что используете `module.exports` (не `export default`) в ecosystem.config.cjs
- Проверьте, что `bun` доступен в PATH: `which bun`
- Проверьте, что файл `server/init.ts` существует в указанной директории

### Проблема: curl <http://localhost> возвращает JSON ошибку от Express

**Симптом:** `{"message":"Cannot GET /","error":"Not Found","statusCode":404}`

**Причина:** Запрос идет напрямую на Express сервер, а не через nginx. Nginx не перехватывает запросы на порту 80.

**Решение:**

```bash
# 1. Проверьте, что nginx запущен
sudo systemctl status nginx

# 2. Проверьте, что nginx слушает на порту 80
sudo ss -tlnp | grep :80
# Должно показать nginx процесс, например:
# tcp   LISTEN  0  128  0.0.0.0:80  0.0.0.0:*  users:(("nginx",pid=123,fd=6))

# 3. Если nginx не слушает порт 80, проверьте конфигурацию
sudo nginx -t

# 4. Проверьте, что конфиг для fit-hub активен
ls -la /etc/nginx/sites-enabled/ | grep -E "fit-hub|gymmy-bot"

# 5. Если конфиг не найден, проверьте sites-available
ls -la /etc/nginx/sites-available/ | grep -E "fit-hub|gymmy-bot"

# 6. Если конфиг есть в sites-available, но нет в sites-enabled:
sudo ln -s /etc/nginx/sites-available/fit-hub /etc/nginx/sites-enabled/

# 7. Или если вы добавляли в существующий конфиг, проверьте что блок добавлен:
sudo grep -A 10 "server_name gymmy-bot.ru" /etc/nginx/sites-available/gymmy-bot.ru

# 8. Перезагрузите nginx
sudo systemctl reload nginx

# 9. Проверьте снова
curl http://localhost
# Теперь должен вернуть HTML или 200 OK, а не JSON ошибку
```

### Проблема: Порт 80 уже занят (Address already in use)

**Симптом:** `nginx: [emerg] bind() to 0.0.0.0:80 failed (98: Address already in use)`

**Причина:** Порт 80 уже используется другим процессом.

**Решение:**

```bash
# 1. Найти процесс, который занимает порт 80
sudo lsof -i :80
# или
sudo ss -tlnp | grep :80
# или
sudo fuser 80/tcp

# 2. Посмотреть что занимает порт
# Обычно это:
# - Другой nginx процесс (может быть запущен вне systemd)
# - Apache
# - Другой веб-сервер

# 3. Если это другой nginx процесс:
ps aux | grep nginx
# Остановите все процессы nginx
sudo systemctl stop nginx
sudo pkill -9 nginx  # если не остановился

# 4. Проверьте, что порт 80 свободен
sudo ss -tlnp | grep :80
# Должно быть пусто

# 5. Запустите nginx снова
sudo systemctl start nginx
sudo systemctl status nginx

# 6. Если порт все еще занят другим приложением:
# Вариант A: Используйте другой порт для fit-hub (8080, 8081 и т.д.)
# Вариант B: Добавьте location блоки в существующий server блок на порту 80
# Вариант C: Настройте существующий server блок для обслуживания fit-hub
```

**Важно:** Если после всех проверок nginx все равно не работает, возможно:

- Другой процесс занимает порт 80
- Nginx конфигурация имеет синтаксические ошибки
- Конфигурация для fit-hub не добавлена или не активна

### Проблема: Nginx выдает 502 Bad Gateway

```bash
# Проверка, запущен ли сервер
pm2 status

# Проверка порта (используйте ваш порт, например 4000)
sudo netstat -tlnp | grep 4000
# или
sudo ss -tlnp | grep 4000

# Проверка, что порт в nginx.conf совпадает с PORT в .env
grep "proxy_pass" /etc/nginx/sites-available/fit-hub
grep "PORT=" /path/to/fit-hub  # ⚠️ ЗАМЕНИТЕ на ваш реальный путь!/.env

# Проверка логов Nginx
sudo tail -f /var/log/nginx/fit-hub-error.log
sudo tail -f /var/log/nginx/error.log
```

### Проблема: Нет папки dist/ или она пустая

Если после `git pull` папка `dist/` отсутствует или пустая:

```bash
# Проверка статуса git
git status

# Проверка, что dist/ не в .gitignore
cat .gitignore | grep dist

# Если dist/ в .gitignore - нужно убрать оттуда (см. раздел 0 - подготовка на локальной машине)
# Затем на локальной машине:
# 1. npm run build
# 2. git add dist/
# 3. git commit -m "Add dist files"
# 4. git push
# И потом на сервере: git pull
```

### Проблема: База данных не подключается

```bash
# Проверка подключения к PostgreSQL
psql -U fit_hub_user -d fit_hub -h localhost

# Проверка .env файла
cat .env | grep DATABASE_URL

# Проверка миграций
bunx prisma migrate status
```

### Проблема: Telegram бот не работает

```bash
# Проверка токена в .env
cat .env | grep TELEGRAM_BOT_TOKEN

# Проверка логов бота
pm2 logs fit-hub | grep -i telegram
```

## Дополнительные команды

```bash
# Просмотр использования ресурсов
pm2 monit

# Сохранение текущей конфигурации PM2
pm2 save

# Удаление из автозапуска
pm2 unstartup

# Просмотр информации о процессе
pm2 describe fit-hub
```
